# Postgres (Railway) — 'postgres://' will be normalized to 'postgresql+psycopg2://'
DATABASE_URL=postgresql://user:pass@host:5432/db

# Dev fallback (optional)
# DATABASE_URL=sqlite:///./local.db
JWT_SECRET=changeme
APP_BASE_URL=http://localhost:8000
PUBLIC_BASE_URL=http://localhost:8000
CPC_DEFAULT_EUR=0.11
CPC_MIN_EUR=0.06
CPC_MAX_EUR=0.20
RISK_VELOCITY_MAX_10M=40
ASN_DENYLIST=[]
COUNTRY_ALLOWLIST=["FR","BE","CA"]
HOLD_DAYS=30
RESERVE_PCT=0.30
ADMIN_TOKEN=changeme-admin
